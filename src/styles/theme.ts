import { extendTheme } from "@mui/joy"

const theme = extendTheme({
  colorSchemes: {
    dark: {
      palette: {
        background: {
          body: "#1a2532",
        },
      },
    },
  },
})

export default theme
